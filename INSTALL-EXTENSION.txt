Gladiator Wallet — update without losing your wallet

PREFERRED UPDATE (keeps seed / accounts):
1. Unzip the new pack
2. Copy everything INSIDE the new folder into your EXISTING Gladiator folder (overwrite)
3. chrome://extensions → Gladiator → Reload (do NOT Remove)
4. If Chrome asks for new permissions (HID / Ledger), Allow
5. Click the toolbar icon once → wait for “Jupiter signing ready”
6. Hard-refresh jup.ag (Ctrl+Shift+R) if you use Jupiter

v0.5.69:
- Connect Ledger (Solana app) from Accounts / Wallets
- Ledger wallets labeled with a Ledger badge (rename in Settings)
- In-wallet Send + Jupiter signing approve on the device

Ledger tips:
- Unlock Nano, open the Solana app, then tap Connect Ledger
- Keep Gladiator open when a dApp asks Ledger to sign

Version: see manifest.json
