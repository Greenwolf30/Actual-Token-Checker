Gladiator Wallet — load as unpacked Chrome/Opera extension

1. chrome://extensions → REMOVE old Gladiator → Load unpacked → THIS folder
2. Click Reload
3. Click the Gladiator TOOLBAR ICON once
4. Wait for toast: “Jupiter signing ready”
5. Hard-refresh jup.ag → Connect → swap

v0.5.55:
- Fixes “No wallet” on sign by caching keys in the extension service worker
  when the toolbar popup opens, then passing them to the invisible signer
- No side window required

If toast does NOT say “Jupiter signing ready”:
- Enter old password if prompted, or import seed/secret key, then reopen popup

Version: see manifest.json
